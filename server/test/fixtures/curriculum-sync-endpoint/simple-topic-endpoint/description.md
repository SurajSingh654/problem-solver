A minimal endpoint-test fixture. Kept separate from the sibling
`curriculum-sync/simple-topic/` fixture so the two integration test files can
run in parallel without stepping on each other in the shared Postgres.
